import { style } from "@vanilla-extract/css";

export const bgWhite = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--white-rgb), var(--opacity))`,
});

export const bgGrey100 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--grey100-rgb), var(--opacity))`,
});

export const bgGrey200 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--grey200-rgb), var(--opacity))`,
});

export const bgGrey300 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--grey300-rgb), var(--opacity))`,
});

export const bgGrey400 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--grey400-rgb), var(--opacity))`,
});

export const bgGrey500 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--grey500-rgb), var(--opacity))`,
});

export const bgGrey600 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--grey600-rgb), var(--opacity))`,
});

export const bgGrey700 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--grey700-rgb), var(--opacity))`,
});

export const bgGrey800 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--grey800-rgb), var(--opacity))`,
});

export const bgGrey900 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--grey900-rgb), var(--opacity))`,
});

export const bgBlue100 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--blue100-rgb), var(--opacity))`,
});

export const bgBlue200 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--blue200-rgb), var(--opacity))`,
});

export const bgBlue300 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--blue300-rgb), var(--opacity))`,
});

export const bgBlue400 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--blue400-rgb), var(--opacity))`,
});

export const bgBlue500 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--blue500-rgb), var(--opacity))`,
});

export const bgBlue600 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--blue600-rgb), var(--opacity))`,
});

export const bgBlue700 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--blue700-rgb), var(--opacity))`,
});

export const bgBlue800 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--blue800-rgb), var(--opacity))`,
});

export const bgBlue900 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--blue900-rgb), var(--opacity))`,
});

export const bgGreen100 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--green100-rgb), var(--opacity))`,
});

export const bgGreen200 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--green200-rgb), var(--opacity))`,
});

export const bgGreen300 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--green300-rgb), var(--opacity))`,
});

export const bgGreen400 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--green400-rgb), var(--opacity))`,
});

export const bgGreen500 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--green500-rgb), var(--opacity))`,
});

export const bgGreen600 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--green600-rgb), var(--opacity))`,
});

export const bgGreen700 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--green700-rgb), var(--opacity))`,
});

export const bgGreen800 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--green800-rgb), var(--opacity))`,
});

export const bgGreen900 = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--green900-rgb), var(--opacity))`,
});

export const bgInfo = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--info-rgb), var(--opacity))`,
});

export const bgPrimary = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--primary-rgb), var(--opacity))`,
});

export const bgSuccess = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--success-rgb), var(--opacity))`,
});

export const bgWarning = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--warning-rgb), var(--opacity))`,
});

export const bgDanger = style({
  vars: { "--opacity": "1" },
  backgroundColor: `rgba(var(--danger-rgb), var(--opacity))`,
});
